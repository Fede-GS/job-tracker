# ── FinixJob Frontend — Environment Variables ─────────────────────────────────
# Copy this file to .env.local for local development.
# On Render, set these in the dashboard under Environment Variables (Build).

# ── API Base URL ──────────────────────────────────────────────────────────────
# Development: leave empty (Vite proxy handles /api → localhost:5000)
# Production:  set to your deployed backend URL, e.g.:
#   VITE_API_BASE_URL=https://finixjob-api.onrender.com/api
VITE_API_BASE_URL=
